import React from 'react';
import ProfileSettings from '../components/ProfileSettings'; // Adjust if path is different

const Settings = () => {
  return (
    <div>
      <ProfileSettings />
    </div>
  );
};

export default Settings;
